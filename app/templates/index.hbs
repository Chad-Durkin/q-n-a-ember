<div class="index">
    <div class="row index-header">
        <h1>Current Questions!</h1><br>
        <hr>
    </div>
    <div class="row">
        {{#each model.questions as |question|}}
            {{question-list question=questions}}
        {{/each}}
    </div>
</div>
